async function ensureSeed() {
  // Demo auto-seeding is disabled so production/admin data stays clean
  // and the first-department onboarding flow can start from an empty state.
  return null;
}

module.exports = ensureSeed;
